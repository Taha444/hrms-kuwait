# ---- الأساس ----
Flask>=3.0,<4.0
Werkzeug>=3.0,<4.0

# ---- خادم إنتاجي (بديل خادم التطوير) ----
waitress>=3.0

# ---- إعدادات البيئة ----
python-dotenv>=1.0

# ---- تصدير المستندات ----
reportlab>=4.0          # توليد PDF (مخالصة، شهادة راتب)
arabic-reshaper>=3.0    # تشكيل النص العربي في PDF
python-bidi>=0.4        # اتجاه النص العربي (RTL) في PDF
openpyxl>=3.1           # تصدير Excel

# ---- المصادقة الثنائية (TOTP) ----
pyotp>=2.9

# ---- الجدولة (فحص التنبيهات اليومي) ----
APScheduler>=3.10

# ---- الاختبارات والجودة (تطوير) ----
pytest>=8.0
