# -*- coding: utf-8 -*-
"""تصحيح أخطاء المسح الضوئي في نصّ العقد الحكومي.

نصّ العقد أُدخل بمسح ضوئي لنموذج الهيئة العامة للقوى العاملة، فحمل آثار
القراءة الآلية: «تسععرى» بدل «تسري»، و«تخت المحكمة» بدل «تختص»، و«باللاتين
العربية» بدل «باللغتين». والمستند يُطبع ويُقدَّم لجهة رسمية بهذه الأخطاء.

**ما صُحِّح وما لم يُصحَّح.** هذه تصحيحات إملائية بحتة لأخطاء لا لبس فيها —
حرف ساقط أو مضاعف أو مسافة مفقودة. لم تُغيَّر صياغة ولا بند ولا رقم قانون:
النصّ نموذج رسمي، وتعديل معناه قرار قانوني لا هندسي.

التصحيح يُطبَّق بالاستبدال على المحفوظ في القاعدة لا بإعادة كتابة النصّ
كاملًا: قاعدة عدّل فيها العميل شيًئا تحتفظ بتعديله، ولا يُمحى بترقية.

Revision ID: r7j8k9l0m1n
Revises: q6i7j8k9l0m
"""
import sqlalchemy as sa
from alembic import op

revision = "r7j8k9l0m1n"
down_revision = "q6i7j8k9l0m"
branch_labels = None
depends_on = None

#: (الخطأ، الصواب) — كلها آثار مسح ضوئي واضحة
TYPOS = [
    # البند الثاني عشر — القانون الواجب التطبيق
    ("تسععرى أحكام", "تسري أحكام"),
    ("في القطالأ الأهلي", "في القطاع الأهلي"),
    ("بشأنه نص ي هذا العقد", "بشأنه نص في هذا العقد"),
    ("كل شرط  م الاتفاق عليه", "كل شرط تم الاتفاق عليه"),
    ("كل شرط م الاتفاق عليه", "كل شرط تم الاتفاق عليه"),
    # البند الرابع عشر — المحكمة المختصة
    ("تخت المحكمة الكلية", "تختص المحكمة الكلية"),
    ("العمالية طبقعا لأحكام", "العمالية طبقًا لأحكام"),
    ("لأحكام القعانون رقم 46", "لأحكام القانون رقم 46"),
    # البند الخامس عشر — لغة العقد
    ("باللاتين العربية", "باللغتين العربية"),
    ("بنصوص اللاة العربية", "بنصوص اللغة العربية"),
    ("عند وقولأ أي تعارع بينهما", "عند وقوع أي تعارض بينهما"),
    # كلمات مفردة: النصّ في المصدر ملتفّ بأسطر، فالمطابقة على
    # العبارة الكاملة تفشل بينما هي في القاعدة سطر واحد.
    ("وقولأ", "وقوع"),
    ("تعارع", "تعارض"),
    # البند السادس عشر — نسخ العقد
    ("من ثلان نسخ", "من ثلاث نسخ"),
    ("للعمل بموجبهعا", "للعمل بموجبها"),
    # البند الرابع — قيمة الأجر
    ("اجرا مقدراه", "أجرًا مقداره"),
    ("سريان هذا القعد", "سريان هذا العقد"),
    # البند الأول والسادس
    ("جزء لا يتجزا  من", "جزء لا يتجزأ من"),
    ("جزء لا يتجزا من", "جزء لا يتجزأ من"),
    ("يخضع لادراه الطرفين", "يخضع لإرادة الطرفين"),
    ("دون اخطار", "دون إخطار"),
    # البند الثامن والتاسع
    ("لا تقلعن ساعة", "لا تقل عن ساعة"),
    ("إلى بلد عند انتهاء", "إلى بلده عند انتهاء"),
    ("ومغاادرته", "ومغادرته"),
    ("نهائيا للبلاد", "نهائيًا للبلاد"),
]


def _apply(conn, direction: str) -> int:
    row = conn.execute(sa.text(
        "SELECT id, body_html FROM document_templates WHERE code = 'GOV-CONTRACT-RENEWAL'"
    )).fetchone()
    if not row or not row[1]:
        return 0
    body = row[1]
    changed = 0
    for wrong, right in TYPOS:
        a, b = (wrong, right) if direction == "up" else (right, wrong)
        if a in body:
            body = body.replace(a, b)
            changed += 1
    if changed:
        conn.execute(sa.text(
            "UPDATE document_templates SET body_html = :b WHERE id = :i"
        ), {"b": body, "i": row[0]})
    return changed


def upgrade() -> None:
    n = _apply(op.get_bind(), "up")
    print(f"[migration r7j8k9l0m1n] صُحِّح {n} خطأ إملائي في نصّ العقد الحكومي")


def downgrade() -> None:
    # التراجع يعيد الأخطاء — مدعوم للاكتمال، ولا سبب وجيًها لاستعماله.
    _apply(op.get_bind(), "down")
