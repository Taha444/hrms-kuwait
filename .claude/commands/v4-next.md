---
description: نفّذ البند التالي بالأولوية
---

```bash
python3 .claude/hrms/scripts/v4.py --next
```

الترتيب المقترح:
```
1. RNW-D1 + RNW-D2   القفلة — معاملات عالقة الآن، والمنع وحده لا ينقذها
2. IMP-01            الانتحال — يعطّل اختبار كل الأدوار
3. TSK-01 + TSK-05   التكرار ثم التنظيف — قبل العدادات
4. TSK-02 + TSK-03   العدادات والفصل — بعد نظافة البيانات
5. SIG-H1            تحقيق سريع قد يوفر شغلا كاملا
6. LBL-*             منخفض المخاطر
7. الباقي
```

**سبب الترتيب**: `RNW-D2` فيه معاملات واقفة الآن. و `IMP-01` يمنع اختبار الأدوار بالانتحال. و `TSK-02` قبل `TSK-01` يعطي عدادا مضبوطا على بيانات مكررة.

اشرح لماذا هذا البند الآن، اقرأ السكيل، نفّذ، سجّل بدليل.
