---
description: أزل القيم والتسميات الداخلية من الواجهة
---

اقرأ سكيل `hrms-label-hygiene`.

1. **(V1.3)**: أزلها من الكتالوج. الاسم المعروض حقل مستقل عن الكود الداخلي.
   لو ظهرت لتمييز نوعين بنفس الاسم، فالمشكلة **نوعان نشطان لنفس الخدمة** — وحّدهما.
2. **`indefinite` وأخواتها**: خريطة ترجمة لكل enum معروض، بالعربي والإنجليزي.
3. **الفحص الشامل**: تصفّح بالعربي وابحث عن أي `snake_case` إنجليزي. ثم بدّل للإنجليزي وكرر.
   غطِّ: القوائم · الفلاتر · التقارير · **التصدير CSV/XLSX** · PDF · الإشعارات · رسائل الخطأ · حالات الفراغ.

**التصدير أكثر ما يُنسى** — مساره غالبا يتجاوز طبقة الترجمة.

تحقق أن `qr` و `gps` و `both` و `none` لم تعد للظهور.

سجّل `LBL-01..04` بدليل.
