---
description: حالة الجولة والحكم
---

```bash
python3 .claude/hrms/scripts/ops.py --report
python3 .claude/hrms/scripts/ops.py --prio P0
python3 .claude/hrms/scripts/ops.py --dod
python3 .claude/hrms/scripts/ops.py --verify
```

اعرض:
1. **الحكم**: كم P0 مفتوح · كم سيناريو نجح · هل الجولة مكتملة
2. نسبة الإغلاق لكل حزمة
3. **التحقيقات غير المحسومة** — هذه تعطّل بنودها
4. البنود `blocked` وما تحتاجه من قرار عمل:
   - `P4-17` مسار الإنقاذ للمعاملات العالقة
   - `P6-27` أي module يكون master في مسار الخروج
   - `P9-32` متى يحتاج السفر مرحلة PRO
   - `P3-13` أي approval chain هي المعتمدة لشهادة الراتب
   - `P5-23` أي مستند يُقبل بتوقيع إلكتروني
5. **البند التالي** ولماذا

اذكر أي بند أُغلق بدليل ضعيف — يحتاج إعادة تحقق قبل الـ Retest.

**واذكر دائما**: المناطق غير المغطاة لم تُختبر ولا تُعتبر سليمة.
