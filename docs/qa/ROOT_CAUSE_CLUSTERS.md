# عناقيد الأسباب الجذرية — 31 ملاحظة ← 9 أسباب

نتيجة Phase 1. التوقّع في الوثيقة كان أن الأسباب أقل بكثير من 31، وهو صحيح:
**تسعة أسباب** تغطّي الـ31، وثلاثة منها تفسّر عشر ملاحظات وحدها.

---

## C1 — تجاوز إداري ضمني في تحديد المُعتمِد ⛔ يمنع الـPilot
**البنود:** QA-01، QA-02
**الموضع:** `workflow.py::can_decide` سطر 497
```python
if not (rt and rt.is_confidential) and user.role in ("company_manager", "company_owner"):
    return True
```
**لماذا:** أُضيف عمدًا (FIX-014) ليتدخّل المدير عند التعطّل، لكنه يجعله معتمِدًا
لكل مرحلة. وQA-02 وجهه الآخر: ما دام المدير يعتمد كل شيء، لم يُختبر مسار
`resolve_stage_approvers` لمسؤول الفرع فعليًا.
**الإصلاح المقترح:** حذف التجاوز، وإن لزم تدخّل إداري فيكون صلاحية مسمّاة
(`override_approval`) تُسجَّل في التدقيق — لا سلوكًا ضمنيًا.

## C2 — حساب الغياب لا يقصّ ولا يميّز ⛔
**البنود:** QA-03، QA-04، (وجزئيًا QA-24)
**الموضع:** `payroll.py` سطر 48–67
**لماذا:** الحلقة تمرّ على كل أيام العمل وتعدّ كل يوم بلا سجل غيابًا. لا قصّ على
`hire_date`/`termination_date`، ولا حالة ثالثة `UNRECORDED`.
**الإصلاح:** حالة `UNRECORDED` منفصلة + قصّ الفترة + سياسة صريحة للخصم.

## C3 — رصيد الإجازة له مصدران ⛔
**البنود:** QA-05
**الموضع:** `Employee.annual_leave_balance` مقابل حساب الاستحقاق في `eos.py`
**لماذا:** رقمان مختلفان بطبيعتهما (متبقٍّ فعلي مقابل مستحق تراكمي) يُعرضان
باسم واحد «رصيد الإجازات».
**الإصلاح:** service واحدة تُعيد الرقمين بأسماء مختلفة صراحة، وكل شاشة تستدعيها.

## C4 — دورة حياة المستند وتاريخ الانتهاء ⛔
**البنود:** QA-06، QA-19، QA-28، QA-14
**لماذا:** OCR يستخرج التاريخ لكن لا يُخزَّن دائمًا على السجل، فمحرك الانتهاء
والعدادات يقرآن حقلًا فارغًا. والإصدارات القديمة غير متاحة.
**الإصلاح:** الـpipeline الكامل في SKILL-6 + backfill.

## C5 — العدادات لا تشارك الاستعلام مع الصفحات التفصيلية
**البنود:** QA-19، QA-20
**الموضع:** `routers/dashboard.py` — `count()` محلية
**الإصلاح:** كل عدّاد يستدعي نفس دالة الصفحة التفصيلية.

## C6 — المسار قالب ثابت لا مشتق من المحتوى
**البنود:** QA-10
**الموضع:** `workflow.py::_chain` يقرأ `approval_chain_json` كما هو
**الإصلاح:** ترشيح المراحل بشروط من الحمولة (سفر خارج البلاد ← مرحلة المندوب).

## C7 — لا فصل بين المهمة والإشعار
**البنود:** QA-11، QA-12، QA-26
**الموضع:** `notifications.py` — كلاهما صف في `tasks`
**الإصلاح:** تمييز النوع + idempotency على `(entity, stage, assignee)` + إغلاق تلقائي.

## C8 — نصوص تقنية تتسرّب للواجهة
**البنود:** QA-07، QA-13، QA-14، QA-17، QA-22، QA-25، QA-27
**لماذا:** لا يمر كل enum على `labels.ts`، وتفاصيل الطلب تُعرض كـJSON خام.
**الإصلاح:** renderer لكل نوع طلب + كنس منهجي.

## C9 — سياسات الوصول موزّعة
**البنود:** QA-21، QA-23، QA-29، QA-16، QA-15، QA-18، QA-09، QA-08
**لماذا:** كل شاشة تقرر ظهورها بشرط مكتوب عندها.
**الإصلاح:** policy واحدة لكل صفحة تُغذّي القائمة والحارس والخادم.

---

## ما أُنجز فعلًا في هذه الجلسة (قبل هذه الوثيقة)

| البند | الحالة | المرجع |
|---|---|---|
| QA-08 | جزئي — نوع `REQSIG` أُنشئ | `7adca3a` — يحتاج تحقق أن الموظف يراه |
| QA-30 | مُنجز | `cd408be` (SEC-01/02/03) |
| QA-31 | **جزئي فقط** | الخمول مطبَّق في الواجهة؛ معيار القبول يشترط رفض الخادم للتوكن |
| QA-29 | جزئي | أُزيلت من HR والمدير؛ المطلوب المندوب **فقط** |
| QA-05 | مهّدت له | `leave_ledger` + خصم تلقائي (`0bc4736`) |
| QA-26 | مهّد له | توسيع `CRITICAL_FIELDS` (`21ffa9c`) |
