# تسليم — إكمال العنقود C4

> ابدأ الجلسة الجديدة بهذه الرسالة:
> **«اقرأ `docs/qa/MASTER_PROMPT.md` و`docs/qa/PROGRESS.md` و`docs/qa/HANDOFF_C4.md`، وكمّل العنقود C4 من حيث توقف.»**

## ما أُنجز (لا تُعده)

الشريحة الأولى في `acfab8f` — **QA-06 حالته `FIXED`**:

- `documents.py::upload_document` يقرأ `expiry_date` من المستند عبر `ocr.extract`
  حين لا يُدخله الرافع. الإدخال اليدوي يفوز دائمًا.
- `documents.py::_sync_permit_from_document` يُبقي `Permit` انعكاسًا لأحدث
  مستند (`residency` / `work_permit`)، ولا يتراجع بتاريخ أقدم.
- `scripts/backfill_document_expiry.py` — تقريري افتراضًا، `--apply` للكتابة.
- أربعة اختبارات باسم `test_qa06_*` في `tests/test_z_r8_r9.py`.

## المتبقّي من C4 — بالترتيب المقترح

### 1. QA-19 + QA-20 — توحيد العدادات (أعلى قيمة)
**الموضع:** `app/routers/dashboard.py::dashboard` — استعلامات `count()` محلية
لا تستدعي دوال الصفحات التفصيلية، فالانحراف بنيوي.

**المطلوب:** كل عدّاد يستدعي نفس الدالة/الاستعلام الذي يغذّي الصفحة التفصيلية.
**الدليل المطلوب:** cross-consistency test يقارن `count()` بـ`len(list())` —
كما في QA-20 حرفيًا.
**الكنس:** كل عدادات لوحة التحكم لا التراخيص وحدها.

### 2. QA-28 — النسخ السابقة من المستندات
**الحالة الفعلية:** التخزين يدعمها بالفعل (`Document.version` + `is_current`)،
والنسخ القديمة **لا تُحذف** — تُعطَّل فقط (`documents.py` سطر ~164).
فالناقص هو **الوصول**: زر «النسخ السابقة» + endpoint تنزيل نسخة بعينها.
تحقق أولًا هل يوجد endpoint للنسخ التاريخية (كان هناك عمل سابق في `21ffa9c`
عن custom docs) قبل إنشاء واحد جديد.

### 3. QA-14 — أسماء المستندات بدل الأكواد
جدول ربط من `document_type_code` إلى اسم بشري. لاحظ R10: النص من i18n.
والتكرار يُحل بعرض التاريخ/الإصدار لا بالحذف (R6).

## تحذيران

**R6 — لا تمسح بيانات.** الإصدارات القديمة تُؤرشف لا تُشال. `_sync_permit_from_document`
مكتوب على هذا الأساس (لا يتراجع بتاريخ أقدم) — حافظ عليه.

**العدادات قد تبدو صحيحة بعد إصلاح QA-06 دون أن تكون موحّدة.** ملء `Permit`
جعل الرقم غير صفري، لكن مصدر العدّاد ما زال منفصلًا عن الصفحة التفصيلية.
لا تُغلق QA-19 لمجرد أن الرقم صار معقولًا — المعيار هو تطابق المصدرين باختبار.

## حالة المشروع

- آخر commit: `acfab8f` · **441/441 اختبار أخضر** · tsc نظيف
- مغلق بأدلة: **QA-01 · 02 · 03 · 04 · 05 · 10 · 30**
- `PROGRESS.md` هو المصدر الرسمي للحالات — حدّثه بعد كل بند.

## معلّق خارج C4 (لا يخصّ هذه الجلسة)

- **الترحيلات لم تُشغَّل على Railway** — 14 ترحيلًا. `db_migrate.py` أُصلح
  (`d1a3f00`) لكن قاعدة الإنتاج قد تكون مختومة عند الرأس زوًرا من العطل القديم.
  الفحص: `SELECT count(*) FROM notification_templates;` — 74 يعني سليم، صفر أو 1
  يعني ختًما زائًفا.
- QA-31 جزئي: الخمول في الواجهة فقط، ومعيار القبول يشترط رفض الخادم للتوكن.
- QA-29 جزئي: أُزيلت الروابط الحكومية من HR والمدير، والمطلوب المندوب **فقط**.
- UI-03 و PERM-07 ينتظران توضيًحا منك.
